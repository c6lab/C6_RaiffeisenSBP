// (c) 2021 C6 Lab, Ramona Orlova, http://c6lab.org
// Все права сохранены.
////////////////////////////////////////////////////////////////////////////////

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Перем ПараметрыФормы;
	ПараметрыФормы = Новый Структура("Ключ", C6_СБПВызовСервера.НайтиНастройки());
	Если (ЗначениеЗаполнено(ПараметрыФормы.Ключ)) Тогда
		ОткрытьФорму("ПланОбмена.C6_СБПНастройки.ФормаОбъекта", ПараметрыФормы);
	КонецЕсли;
КонецПроцедуры
