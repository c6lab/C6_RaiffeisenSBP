
&Перед("ВыполнитьОткрытиеПечатнойФормы")
Процедура C6_ВыполнитьОткрытиеПечатнойФормы(ИсточникДанных, ИдентификаторКоманды, ОбъектыНазначения, Форма, СтандартнаяОбработка)
	Если (ИдентификаторКоманды = "ВнешняяОбработка.C6ПримерСБП") Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
КонецПроцедуры
